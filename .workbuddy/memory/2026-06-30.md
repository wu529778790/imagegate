# 2026-06-30

## 删除小红书和信息图页面
- 从 `AppHeader.tsx` 的 `NAV_ITEMS` 中移除了「小红书」(`/xhs`) 和「信息图」(`/infographic`) 两个导航项
- 删除了 `src/app/xhs/` 和 `src/app/infographic/` 两个页面目录
- 导航栏现在只保留：生图 `/`、图库 `/gallery`、记录 `/records`

## 全面前端重构

### 新增共享类型体系 (`src/types/`)
- `providers.ts` — ProviderMeta, PROVIDER_COLORS, PROVIDER_LABELS, PROVIDERS 统一定义
- `generation.ts` — AR_OPTIONS, QUALITY_OPTIONS, GenerateParams 集中管理
- `records.ts` — GenerationRecord, RecordStatus, STATUS_CONFIG 共享
- `images.ts` — ImageItem, SyncStatus 对齐真实 API 返回
- `settings.ts` — UserSettings 类型 + Quality 重导出

### 新增通用工具 (`src/lib/utils.ts`)
- `downloadImage()` — 统一图片下载逻辑
- `copyToClipboard()` — 统一剪贴板操作
- `formatDuration()` / `formatRelativeTime()` — 时间格式化

### CSS 体系统一
- 新增 CSS 变量：`--header-height`(52px), `--sidebar-width`(280px), `--accent-gradient`
- `.nav-link.active` 背景改用 `var(--nav-active-bg)`
- `AppHeader.tsx`: `<style jsx>` → CSS Module (`AppHeader.module.css`)
- `login/page.tsx`: 内联样式 → CSS Module (`Login.module.css`)
- `Home.module.css`: `calc(100vh - 52px)` → `calc(100vh - var(--header-height))`

### 组件重构
- `GenerateParams` — 移除本地 AR_OPTIONS/PROVIDER_COLORS/PROVIDER_LABELS，改为 `@/types` 导入
- `GenerateResult` — 使用 `@/types` + `@/lib/utils`，移除内联 download 逻辑
- `TagBadge` — StatusBadge 使用共享 `STATUS_CONFIG`，ProviderBadge 使用共享 `PROVIDER_COLORS`
- `SettingsModal` / `ProviderForm` — 使用 `ProviderMeta` 共享类型
- `AppHeader` — CSS Module 替代 styled-jsx，图标区分度（生图/图库/记录不同图标），头像下拉菜单添加样式
- `AuthContext` — 移除 `setTimeout` 硬编码依赖，用 `useRef` 管理 Promise
- `AuthModal` — 修复 `mask.closable`→`maskClosable`, `destroyOnHidden`→`destroyOnClose`，修复 `/settings` 404→自定义事件
- `ThemeProvider` — 手写 SkipLink → 使用 `SkipLink` 组件
- `EmptyState` — `EmptyStates` 从 JSX 元素改为纯配置对象，新增 `icon` 属性

### 四个页面全面重写
- `page.tsx` — 使用 `@/types` 类型，`downloadImage` 工具函数，清理硬编码 AR_OPTIONS
- `login/page.tsx` — CSS Module 全面替代内联样式
- `gallery/page.tsx` — 使用 `ImageItem`/`SyncStatus` 共享类型，清理未用 import
- `records/page.tsx` — 使用 `GenerationRecord`/`STATUS_CONFIG`/`PROVIDER_LABELS` 共享类型

### 清理未使用代码
- 删除组件：Container, Grid, SelectableCard, ActionButtons, AccessibleButton, VirtualList（含 CSS）
- `ui/index.ts` 只导出实际使用的组件
- 移除 SectionTitle/MainContent 等未使用导出

### 编译状态
- TypeScript: 零错误通过
- ESLint: 剩余 error 为预存 `any` 类型（lib/rate-limit.ts, lib/validation.ts, providers/），warnings 为预存未使用变量
